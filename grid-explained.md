
# 🧱 CSS Grid – סיכום מוסבר (שורה־שורה)

CSS Grid הוא מנגנון פריסה שמאפשר לבנות **מבנים דו־ממדיים** – שורות ועמודות יחד.  
מעולה לדפים שלמים, לייאאוטים מורכבים ודשבורדים.

---

## הפעלת Grid

```css
display: grid;
```
הופך את האלמנט ל־Grid Container.  
כל הילדים שלו מסתדרים בתוך **טבלה של שורות ועמודות**.

---

## הגדרת עמודות – grid-template-columns

```css
grid-template-columns: 200px 200px 200px;
```
יוצר 3 עמודות קבועות ברוחב 200px.

```css
grid-template-columns: 1fr 1fr 1fr;
```
יוצר 3 עמודות שוות שמתחלקות ברוחב המסך.

```css
grid-template-columns: 200px 1fr;
```
עמודה ראשונה קבועה, שנייה גמישה.

---

## הגדרת שורות – grid-template-rows

```css
grid-template-rows: 100px 1fr;
```
שורה ראשונה בגובה קבוע, שנייה גמישה.

---

## יחידת מידה fr

```css
grid-template-columns: 2fr 1fr;
```
העמודה הראשונה תקבל פי 2 מקום מהשנייה.

---

## רווחים בין תאים – gap

```css
gap: 20px;
```
יוצר רווח אחיד בין כל השורות והעמודות.

```css
row-gap: 10px;
column-gap: 30px;
```
שליטה נפרדת על רווח שורות ועמודות.

---

## קיצור דרך – repeat

```css
grid-template-columns: repeat(3, 1fr);
```
בונה 3 עמודות שוות בלי לחזור על עצמך.

---

## התאמה אוטומטית – auto-fit / auto-fill

```css
grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
```
יוצר כמה עמודות שנכנסות למסך, וכל אחת לפחות 200px.

---

## יישור כללי – justify-items / align-items

```css
justify-items: center;
```
מיישר את התוכן בתוך התא אופקית.

```css
align-items: center;
```
מיישר את התוכן בתוך התא אנכית.

---

## יישור כל הגריד – justify-content / align-content

```css
justify-content: center;
```
ממרכז את כל הגריד אופקית.

```css
align-content: center;
```
ממרכז את כל הגריד אנכית.

---

# פקודות על הילדים (Grid Items)

## מיקום תא – grid-column / grid-row

```css
grid-column: 1 / 3;
```
האלמנט יתפרס מעמודה 1 עד 3.

```css
grid-row: 1 / 2;
```
האלמנט יהיה בשורה הראשונה בלבד.

---

## קיצור דרך – grid-area

```css
grid-area: 1 / 1 / 3 / 3;
```
סדר הערכים:
שורה התחלה / עמודה התחלה / שורה סוף / עמודה סוף.

---

## שמות אזורים – grid-template-areas

```css
grid-template-areas:
  "header header"
  "menu content"
  "footer footer";
```
מגדיר אזורים עם שמות.

```css
grid-area: header;
```
משייך אלמנט לאזור בשם header.

---

## יישור ילד ספציפי – justify-self / align-self

```css
justify-self: center;
```
ממרכז את האלמנט בתוך התא אופקית.

```css
align-self: end;
```
מיישר את האלמנט לתחתית התא.

---

## סיכום קצר
- display: grid – מפעיל Grid  
- grid-template-columns – עמודות  
- grid-template-rows – שורות  
- fr – חלוקה חכמה של מקום  
- gap – רווחים  
- grid-column / row – מיקום  
- grid-template-areas – לייאאוט בשמות  

Grid = הכלי החזק לבניית מבנה של עמוד שלם.
